[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Removed manual redirects as the plugin handles them automatically for Next.js App Router.
# If you are still seeing 404s, ensure that your 'Base directory' in Netlify is set to 'web'
# or that you initialized git inside the 'web' folder (which you did).
